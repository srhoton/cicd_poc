version: 2.1

orbs:
  aws-ecr: circleci/aws-ecr@8.2.1
  aws-ecs: circleci/aws-ecs@3.2.0

workflows:
  build-and-deploy:
    jobs:
      - aws-ecr/build-and-push-image:
          create-repo: false
          dockerfile: Dockerfile
          executor: aws-ecr/default
          platform: linux/amd64
          push-image: true
          repo: cicd-poc
          repo-scan-on-push: true
          registry-id: "705740530616"
          tag: latest
